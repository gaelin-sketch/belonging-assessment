---
category: Charts
---

# Bar

A 100% stacked bar — the workhorse chart, built from CSS not a charting library. Segment colours come from the fixed scales and are never reassigned. `ariaLabel` is required because the printed percentages are decorative and clip.
